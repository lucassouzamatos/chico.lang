fun recur (F) -> 
  fun Recursion (X) ->
    apply X X done
  done

  fun Bind (X) ->
    fun Inner (Y) ->
      let A = apply X X done
      apply A Y done
    done

    apply F Inner done
  done

  apply Recursion Bind done
done