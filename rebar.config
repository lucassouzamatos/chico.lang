{erl_opts, [debug_info]}.

{deps, []}.

{shell, [{apps, [chico]}]}.

{plugins, [steamroller]}.

{
  steamroller,
  [
    {line_length, 100},
    {indent, 2},
    {inputs, ["rebar.config", "{src,tests,include}/*.{[he]rl,app.src}"]}
  ]
}.
