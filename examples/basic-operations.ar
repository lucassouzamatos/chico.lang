apply + 3 5
apply + 5 9
apply * 9 0
apply / 10 2

# functions definition
var sum := (a b) -> apply + a b
var result := apply sum 1 2

# variable declaration
var a := 1
var b := 2

# factorial implementation
var factorial := 
  (number index) where number == 0 ->
    1
  (number index) where number < 0 -> 
    .
  (number index)
    var x := apply - number 1
    var y := apply factorial x

    apply * number y

